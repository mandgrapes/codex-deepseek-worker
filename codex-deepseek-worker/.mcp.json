{
  "mcpServers": {
    "dsbro": {
      "command": "powershell.exe",
      "tool_timeout_sec": 3600,
      "args": [
        "-WindowStyle",
        "Hidden",
        "-NoProfile",
        "-NonInteractive",
        "-Command",
        "$key = [Environment]::GetEnvironmentVariable('DEEPSEEK_API_KEY', 'User'); if ([string]::IsNullOrWhiteSpace($key)) { throw 'DEEPSEEK_API_KEY is not configured.' }; $env:DEEPSEEK_API_KEY = $key; & codex mcp-server; exit $LASTEXITCODE"
      ]
    }
  }
}
